{
  "name": "ecommmer-aidlc",
  "version": "0.0.0",
  "private": true,
  "description": "Chat-Native E-Commerce Platform — Multi-Agent MVP",
  "engines": {
    "node": ">=22.0.0 <23.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "ci": "bash scripts/ci.sh",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "gen:contracts": "echo 'TODO: contract generator wired in UoW-02'",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "typescript": "5.6.2",
    "@redocly/cli": "1.25.5"
  },
  "pnpm": {
    "overrides": {
      "postcss": ">=8.5.10"
    }
  }
}
